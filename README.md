# Blockchain
Blockchain  Library

##### php扩展条件
```
SYMP_CODE_18755553669
php -v :
    php最好使用7
php -m 查看 
    bcmath calendar Core ctype curl(版本等于大于 7.55.1)
    date dom fileinfo filter gd(需要打开)
    hash iconv json libxml mbstring mcrypt mysqli mysqlnd openssl
    pcre PDO pdo_mysql Phar readline redis(版本3.1.2以上) Reflection
    session SimpleXML SPL standard tokenizer wddx xml xmlreader xmlwriter zip zlib
```